name: test

channels:
  - conda-forge

dependencies:
  # Base depends
  - python
  - pip
  - pillow
  - pmw
  - seamm-util

  # Testing
  - black
  - codecov
  - flake8
  - pytest
  - pytest-cov

  # Documentation
  - pygments

  # Pip-only installs
  - pip:
    # Documentation
    - rinohtype
    - sphinx-rtd-theme
    - pystemmer
