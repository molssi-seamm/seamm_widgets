name: test

channels:
  - conda-forge

dependencies:
  # Base depends
  - python
  - pip
  - pmw

  # Testing
  - black
  - codecov
  - flake8
  - pytest
  - pytest-cov

  # Documentation
  - pygments

  # Pip-only installs
  - pip:
    - seamm-util

    # Documentation
    - rinohtype
    - sphinx-rtd-theme
    - pystemmer
