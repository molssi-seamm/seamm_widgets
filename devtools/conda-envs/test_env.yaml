name: test

channels:
  - default

dependencies:
    # Base depends
  - python
  - pip

    # Testing
  - coverage
  - flake8
  - pytest
  - pytest-cov
  - pytest-runner

  # Pip-only installs
  - pip:
    - Pmw
    - seamm-util>=0.7

      # Testing
    - codecov
    - yapf

