name: test

channels:
  - conda-forge

dependencies:
  # Base depends
  - python
  - pip

  # Testing
  - codecov
  - flake8
  - pytest
  - pytest-cov

  # Pip-only installs
  - pip:
    - Pmw
    - seamm-util

      # Testing
    - yapf

