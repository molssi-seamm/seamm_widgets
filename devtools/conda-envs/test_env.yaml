name: test

channels:
  - conda-forge

dependencies:
  # Base depends
  - python
  - pip

  # Testing
  - black
  - codecov
  - flake8
  - pytest
  - pytest-cov

  # Pip-only installs
  - pip:
    - Pmw
    - seamm-util

      # Testing
    - yapf

    # Documentation
    - rinohtype
    - pygments
    - sphinx-rtd-theme
    - pystemmer
